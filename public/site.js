const menuButton=document.querySelector('.menu-button');const nav=document.querySelector('#primary-nav');if(menuButton&&nav){menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open);document.body.classList.toggle('menu-open',!open)});nav.addEventListener('click',event=>{if(event.target.closest('a')){menuButton.setAttribute('aria-expanded','false');nav.classList.remove('open');document.body.classList.remove('menu-open')}})}
const marketButtons=document.querySelectorAll('[data-market]');const marketPanels=document.querySelectorAll('[data-market-panel]');marketButtons.forEach(button=>button.addEventListener('click',()=>{marketButtons.forEach(item=>item.classList.toggle('active',item===button));marketPanels.forEach(panel=>panel.hidden=panel.dataset.marketPanel!==button.dataset.market)}));
document.querySelectorAll('[data-form]').forEach(form=>form.addEventListener('submit',async event=>{event.preventDefault();const status=form.querySelector('.form-status');const button=form.querySelector('[type=submit]');status.classList.remove('error');status.textContent='Sending...';button.disabled=true;try{const data=Object.fromEntries(new FormData(form));data.consent=Boolean(data.consent);data.policyConsent=Boolean(data.policyConsent);data.turnstileToken=document.querySelector('[name="cf-turnstile-response"]')?.value||'';const response=await fetch(form.action,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const result=await response.json().catch(()=>({}));if(!response.ok)throw new Error(result.error||'We could not send your request. Please try again.');window.location.assign(form.dataset.form==='enrollments'?'/checkout/?enrollmentId='+encodeURIComponent(result.enrollmentId):'/thank-you/')}catch(error){status.classList.add('error');status.textContent=error.message}finally{button.disabled=false}}));

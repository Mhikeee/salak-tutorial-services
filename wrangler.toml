name = "salak-tutorial-services"
pages_build_output_dir = "./dist"
compatibility_date = "2026-08-03"

# Add a Cloudflare D1 binding named DB in the Pages dashboard before enabling forms.
# Keep IDs and secrets out of this repository.
